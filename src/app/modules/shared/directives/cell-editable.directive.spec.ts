import { CellEditableDirective } from './cell-editable.directive';

describe('CellEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new CellEditableDirective();
    expect(directive).toBeTruthy();
  });
});
