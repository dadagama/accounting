export interface FormattedRecord {
    date: string;
    description?: string;
    productImage?: string;
    productName: string;
    productId?: string;
    sellerId?: string;
    sellerName?: string;
    selected?: boolean;
    // ticketId?: string;
    uuid?: string;
}
