export interface FormattedRecord {
    date: string;
    description?: string;
    price: number;
    productImage?: string;
    productName: string;
    productId?: string;
    sellerId?: string;
    sellerName?: string;
    selected?: boolean;
    // ticketId?: string;
    uuid?: string;
}
